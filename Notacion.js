/ Notaciones: ; . []  {}

// ; delimita el final de una línea
var variable;

// . --> Se utiliza en los objetos para acceder a los atributos, ej: movil.anchura


//[] se utiliza para las listas o arrays o acceder a algún valor dentro de la lista
let numeros = [2, 4, 6, 8];
console.log(numeros[2])

//() se utiliza para las funciones
function suma(a, b) {
  // Se escribe la funcion
}


//{} para objetos, funciones y estructuras de control
const movil = {
  alto: 10,
  largo: 5
}
